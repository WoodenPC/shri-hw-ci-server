{
  "name": "pushserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build-docker-image": "sudo docker build -t push-server .",
    "build-docker-image:dev": "sudo docker build -f ./Dockerfile.dev -t push-server ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.3",
    "prettier": "^2.0.5",
    "web-push": "^3.4.3"
  }
}
